# Artifacts

Durable stage outputs should be stored here and reused by downstream steps by default.

- `raw/`: raw discovery and scraping outputs
- `processed/`: cleaned corpora and chunk manifests
- `generated/`: model outputs and prompt-linked image files
- `evaluations/`: comparison outputs and scoring summaries
- `manifests/`: reusable stage manifests and workflow traces

